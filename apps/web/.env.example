# Set secrets from apps/web with:
#   pnpm exec dotenvx set KEY value
#
# Encrypted values land in .env and are committed.
# .env.keys stays local and is gitignored.

DATABASE_URL=file:local.db
VITE_SENTRY_DSN=
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com
