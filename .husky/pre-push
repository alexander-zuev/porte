# Turbo's terminal UI takes mouse reporting and can leak it on a failed exit;
# a hook needs plain streamed output, not a TUI.
export TURBO_UI=stream

pnpm test:integration
pnpm test:live
pnpm test:design || echo "test:design failed (non-blocking)"
